name: Auto-Merge

on:
  pull_request:
    types: [labeled]
  workflow_run:
    workflows: ["CI", "PR Quality Checks"]
    # "E2E (Playwright)" — excluded: known failing suite, not a merge prerequisite
    types: [completed]
  schedule:
    # Last-resort fallback poller — ci.yml and pr-quality.yml each dispatch this
    # workflow directly via workflow_dispatch the moment their own jobs finish
    # (reliable, since it's a direct API call rather than workflow_run chaining,
    # which only reliably fires for push-triggered runs, not pull_request-triggered
    # ones). This cron only matters if that dispatch itself somehow fails to fire.
    - cron: '*/5 * * * *'
  workflow_dispatch:

permissions:
  pull-requests: write
  contents: write
  checks: read
  workflows: write

jobs:
  merge:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/github-script@v7
        with:
          script: |
            async function tryMerge(prNumber, headSha) {
              // Fetch all check runs for this commit (excludes this job itself by name)
              const { data } = await github.rest.checks.listForRef({
                owner: context.repo.owner,
                repo: context.repo.repo,
                ref: headSha,
                per_page: 100,
              })
              // "E2E Tests (Playwright)" is also excluded — known failing suite, not a merge prerequisite
              const checks = data.check_runs.filter(r =>
                r.name !== 'merge' &&
                r.name !== 'E2E Tests (Playwright)'
              )

              if (checks.length === 0) {
                console.log(`PR #${prNumber}: no checks found — CI may not have run yet`)
                return false
              }

              const pending = checks.filter(r => r.status !== 'completed')
              if (pending.length > 0) {
                console.log(`PR #${prNumber}: still pending: ${pending.map(r => r.name).join(', ')}`)
                return false
              }

              const failed = checks.filter(r =>
                !['success', 'skipped', 'neutral'].includes(r.conclusion ?? '') &&
                !r.name.startsWith('codecov/')
              )
              if (failed.length > 0) {
                console.log(`PR #${prNumber}: failed checks: ${failed.map(r => `${r.name}=${r.conclusion}`).join(', ')}`)
                return false
              }

              // No blocking reviews
              const reviews = await github.paginate(github.rest.pulls.listReviews, {
                owner: context.repo.owner,
                repo: context.repo.repo,
                pull_number: prNumber,
              })
              if (reviews.some(r => r.state === 'CHANGES_REQUESTED')) {
                console.log(`PR #${prNumber}: changes requested — skipping auto-merge`)
                return false
              }

              console.log(`PR #${prNumber}: all checks pass — squash-merging`)
              try {
                await github.rest.pulls.merge({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  pull_number: prNumber,
                  merge_method: 'squash',
                })
                console.log(`PR #${prNumber}: merged successfully`)
                return true
              } catch (e) {
                console.log(`PR #${prNumber}: merge failed (may already be merged): ${e.message}`)
                return false
              }
            }

            if (context.eventName === 'schedule' || context.eventName === 'workflow_dispatch') {
              // Fallback poller — check every open, non-draft PR carrying the auto-merge label
              const prs = await github.paginate(github.rest.pulls.list, {
                owner: context.repo.owner,
                repo: context.repo.repo,
                state: 'open',
                per_page: 100,
              })
              const candidates = prs.filter(p => !p.draft && p.labels.some(l => l.name === 'auto-merge'))
              console.log(`Scheduled sweep: ${candidates.length} open PR(s) with the auto-merge label`)
              for (const pr of candidates) {
                await tryMerge(pr.number, pr.head.sha)
              }
              return
            }

            let prNumber, headSha

            if (context.eventName === 'pull_request') {
              // Triggered by label being applied to PR
              if (context.payload.label.name !== 'auto-merge') return
              if (context.payload.pull_request.draft) return
              prNumber = context.payload.pull_request.number
              headSha = context.payload.pull_request.head.sha
            } else {
              // Triggered by CI completing — find an open non-draft PR at this SHA with auto-merge label
              const sha = context.payload.workflow_run.head_sha
              const branch = context.payload.workflow_run.head_branch
              const { data: prs } = await github.rest.pulls.list({
                owner: context.repo.owner,
                repo: context.repo.repo,
                state: 'open',
                head: `${context.repo.owner}:${branch}`,
              })
              const pr = prs.find(p =>
                p.head.sha === sha &&
                !p.draft &&
                p.labels.some(l => l.name === 'auto-merge')
              )
              if (!pr) return
              prNumber = pr.number
              headSha = sha
            }

            await tryMerge(prNumber, headSha)
